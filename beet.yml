name: Instaslate
version: v1.1.0
output: build

data_pack:
  description: "Instamine Deepslate with Eff 5 Netherite Pickaxe + Haste 2.."
  min_format: 101
  max_format: 101
  zipped: true
  load:
    - src

require:
  - bolt
  - eroxified2

pipeline:
  - mecha